
services:
  postgres:
    image: postgres:latest
    container_name: wallet_service_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DATABASE: wallet_service_db
      PGDATA: /var/lib/data/postgresql